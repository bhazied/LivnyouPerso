parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phplint: ~
        phpcsfixer2: ~
        phpmd:
            exclude: ['src/ContinuousNet/LivnYouBundle/Entity']
            ruleset: ['design', 'naming', 'unusedcode', 'controversial']
            triggered_by: ['php']
