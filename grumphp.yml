parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phplint: ~
        phpcsfixer2: ~
        phpmd:
            exclude: []
            ruleset: ['cleancode',  'naming']
            triggered_by: ['php']
